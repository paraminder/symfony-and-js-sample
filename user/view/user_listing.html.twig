{% extends 'inner-common.html.twig' %}

{% block body %}
{# you can read and display just one flash message type... #}
{% for flash_message in app.session.flashBag.get('notice') %}
    <div class="alert alert-success">
        {{ flash_message }}
    </div>
{% endfor %}
{% for flash_message in app.session.flashBag.get('warning') %}
    <div class="alert alert-danger">
        {{ flash_message }}
    </div>
{% endfor %}
<div class="bs-example table-responsive company-listing-detail" data-example-id="striped-table">
<h2 class="page-header">User Listing 
{% if 'ADD_USER' in sessionPermission %}
  <a href="{{ base_url }}company/{{companyDetail.getId()}}/user/create" class="btn btn-primary btn-sm pull-right"><i class="glyphicon glyphicon-plus"></i> Add User</a>
{%endif%}

</h2>  <table style="width:100%;" id="userListing" class="table table-striped table-condensed">
        <thead>
            <tr>
                <th>#</th>
              <th>First Name</th>
              <th>Last Name</th>
              <th>User Email</th>
               <th>Actions</th>
            </tr>
        </thead>
        <!--<tfoot>
            <tr>
                <th>#</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>User Email</th>
               <th>Actions</th>
            </tr>
        </tfoot>-->
    </table>

</div>
{% endblock %}
